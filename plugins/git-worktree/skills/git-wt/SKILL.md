---
description: Use Git worktree according to the task via `git-wt` cli
---

## Task

- Assess given task and current branch, use `git wt` subcommand to split working branch(worktree) appropriately.

### 


## Overview of CLI

- list up usable worktrees: !`git wt`
- create worktree: `git wt ${new-branch-name}`

## Additional resources

- For complete API details, see [reference](reference.md)
